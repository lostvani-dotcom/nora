# URL do servidor embutida no APK — o usuário baixa, instala e usa, sem configurar nada.
# Se a URL do seu deploy no Render for diferente, atualize aqui e recompile o APK.
# (No app, o botão "Configurar servidor" na tela de login continua existindo como alternativa.)
VITE_API_URL=https://nora-jqe2.onrender.com
